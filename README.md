# Zero-Inflated-LCRM-Model
The codes are written in both Rcpp and R software. First run the Rcpp code in the R console, then run the R code to get the estimates of the model parameters and also the summary table.
